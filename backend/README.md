# CivicConnect backend layer

This is the current Express static-serving layer. It serves the compiled frontend and client-side routes. Authentication, database persistence, email delivery, and API endpoints are not implemented in this static build.
