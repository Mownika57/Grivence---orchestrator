# CivicConnect GitHub Pages deployment

This folder is a self-contained static build of CivicConnect. It has no Manus preview wrapper or preview watermark.

Configure GitHub Pages to deploy from the `main` branch and `/docs`.
